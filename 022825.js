    // ******MY ANSWER********

    function multipleOfIndex(arr) {
        const result = arr.filter((e,i) => e===0  || e%i===0);
        
        return result
          // good luck
        }
        
        // ***this is a re-do from yesterday's problem